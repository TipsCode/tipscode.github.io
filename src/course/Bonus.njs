import Nullstack from 'nullstack';

class Bonus extends Nullstack {
    render() {
        return (
            <div>
                Hello, World!
                By Bonus section
            </div>
        )
    }
}

export default Bonus;